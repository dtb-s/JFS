package lesson11;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/**
 * 练习 1 测试：CalculatorTest
 * <p>
 * TODO：写 5 个测试方法（断言失败时看 expected/but was 信息）：
 * 1. addShouldReturnSum：add(2, 3) == 5
 * 2. subtractShouldReturnDifference：subtract(10, 4) == 6
 * 3. multiplyShouldReturnProduct：multiply(3, 4) == 12
 * 4. divideShouldReturnQuotient：divide(10, 4) == 2.5
 * 5. divideByZeroShouldThrow：assertThrows(ArithmeticException.class, () -> calc.divide(1, 0))
 * <p>
 * 建议：用 @BeforeEach 初始化 Calculator。
 */
class CalculatorTest {

    // 你的代码
}